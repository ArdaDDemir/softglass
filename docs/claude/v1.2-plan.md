# Softglass v1.2 — Uygulama planı

> **Durum:** Plan + 1.2a done + 1.2b DatePicker (branch). 1.1.0 shipped.  
> **Checkbox özeti:** [todos.md](./todos.md)  
> **Çalışma:** her sprint yeni git worktree; push/PR kullanıcı onayı; hesap = **ArdaDDemir** / npm **ardaddemir**.

---

## 1) Bugün neredeyiz?

| | |
|--|--|
| **Shipped** | `v1.1.0` — overlays, exit motion, registry, Combobox/MultiSelect |
| **npm** | `@softglass/tokens@1.1.0` + `@softglass/ui@1.1.0` |
| **GitHub** | [ArdaDDemir/softglass](https://github.com/ArdaDDemir/softglass) tag `v1.1.0` |

---

## 2) v1.2 ne olsun?

**Önerilen tanım:**

> **1.2.0** = ContextMenu + DatePicker (form/overlay tamamlama) + minimum Vitest iskeleti  
> (+ mümkünse overlay portal polish). Storybook/docs site **zorunlu değil**.

| Release | İçerik |
|---------|--------|
| **1.2.0** | 1.2a + 1.2b + 1.2c (ContextMenu, DatePicker, tests) |
| **1.2.x** | 1.2d portal polish |
| **1.3** | Docs site, visual regression, theme builder |

---

## 3) Sprint detayları

### 1.2a — ContextMenu

| | |
|--|--|
| **Neden** | DropdownMenu var; sağ tık menüsü yok — kit yarım hissi |
| **DoD** | Export + playground + docs + registry + typecheck/build |
| **API** | DropdownMenu `items` diline yakın tut (öğrenme maliyeti düşük) |
| **Yapma** | Nested submenu ağacı |

### 1.2b — DatePicker

| | |
|--|--|
| **Neden** | Form kit’te en çok aranan eksik; Combobox/MultiSelect sonrası doğal |
| **DoD** | Tek tarih, klavye, field meta, 4 dilde okunur UI |
| **Yapma** | Range, time, locale pack, full i18n |

### 1.2c — Test iskeleti

| | |
|--|--|
| **Neden** | Publish güveni; regression’ı erken yakala |
| **DoD** | Vitest çalışır; ≥3 smoke test; CI yeşil |
| **Yapma** | Coverage tiyatrosu, E2E farm |

### 1.2d — Overlay portal (should)

Absolute menüler hâlâ parent stacking’e bağlı (1.1’de z-index yama var).  
Portal + collision = gerçek ürün edge case.

### 1.2e — Nice

Card tilt, label-float, creatable combobox, README GIF, token JSON — 1.2’yi geciktirmesin.

---

## 4) Teknik notlar

- **Hesap:** gh `ArdaDDemir`, npm `ardaddemir` / org `softglass`  
- **Registry:** yeni component → `scripts/generate-registry.mjs` + `registry:validate` + `registry:build`  
- **Motion:** exit için mevcut `usePresence`  
- **Tokens:** DatePicker/ContextMenu yüzeyleri Select/menu frost dilini yeniden kullansın  

---

## 5) ADHD / odak

- Aynı anda **tek sprint**  
- Yan fikir → todos “v1.3+ backlog”  
- Review: yazan session’da deep review yok  

---

## 6) İlk adım (yeni session)

1. `main` pull / worktree `feat/v1.2-context-menu`  
2. Sprint **1.2a ContextMenu** only  
3. Bitince self-review → 1.2b  

**Kod bu planda yok.**
