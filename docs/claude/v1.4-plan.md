# Softglass v1.4 — Molekül seti planı

> **Durum:** Plan (kod yok). Onaydan sonra sprint sprint uygulanır.  
> **Checkbox özeti:** [todos.md](./todos.md)  
> **Base:** `v1.3.0` shipped (npm + tag + branch `feat/v1.3-progress`).  
> **Çalışma:** her sprint kendi worktree + branch; **otomatik push/PR yok**.  
> **Hesap:** GitHub **ArdaDDemir** · npm **ardaddemir** — work/Feedemy **YASAK**.  
> **Kural:** Aynı anda **tek sprint**. Molecule bitmeden organism (1.5) yok.

---

## 1) Bugün neredeyiz?

| | |
|--|--|
| **Shipped** | `v1.3.0` — atom katmanı kapanışı (Must + Should + Nice) |
| **npm** | `@softglass/tokens@1.3.0` + `@softglass/ui@1.3.0` |
| **GitHub** | [ArdaDDemir/softglass](https://github.com/ArdaDDemir/softglass) · tag `v1.3.0` |
| **Atoms** | ~60+ export (Progress, Slider, Chip, Pin, TimeInput, …) |
| **Molecules (şimdi)** | Card, Modal, Popover, DropdownMenu, ContextMenu, Tabs, Toast |
| **Organism** | AppShell (+ ShellNav) |

**Boşluk:** nav/disclosure, empty/side-panel surface, structure composites, DatePicker portal.

---

## 2) Ürün cümlesi

> **1.4.0** = atomların üstünde **ürün dilinde moleküller**: açılır/nav, empty/sheet yüzeyleri, basit yapı (stepper/toolbar/list/stat) + form/overlay polish.

| Release | Cümle |
|---------|--------|
| **1.3.0** | Atom katmanı kapanır *(shipped)* |
| **1.4.0** | Molekül seti: Accordion, Breadcrumb, Pagination, EmptyState, Sheet, HoverCard, Stepper, Toolbar, List, Stat + form polish |
| **1.5.0** | Organism & product patterns (shell depth, PageHeader, CommandPalette minimal) |
| **1.6+** | Docs site / visual / theme builder |

---

## 3) Bilinçli YAPMA (1.4)

| Yapma | Neden |
|-------|--------|
| DataTable / virtualization | Ayrı major; 1.6+ |
| Command palette full | 1.5b minimal; full 1.6 |
| Menubar OS parity | Scope şişirir |
| Nested modal stack | LIMITATIONS bilinçli |
| Icon pack / full Typography | Atom kararı; 1.3’te kapandı |
| Framer Motion dependency | CSS-first sözleşme |
| Theme builder SaaS | 1.6 vitrin |
| Date **range** suite | 1.5/1.6; tek DatePicker portal yeter |
| Storybook + Chromatic farm | 1.6 |
| Şirket hesabı push/publish | Disiplin |

---

## 4) Sprint haritası

```
[ 1.4-0 ]  Hero + plan hijyeni (docs-only, bu branch)
        ↓
[ 1.4a  ]  Disclosure & nav     → Accordion/Collapsible · Breadcrumb · Pagination
        ↓
[ 1.4b  ]  Surface              → EmptyState · Sheet/Drawer · HoverCard
        ↓
[ 1.4c  ]  Structure            → Stepper · Toolbar · List · Stat
        ↓
[ 1.4d  ]  Form / overlay polish → DatePicker portal · MultiSelect filter · Combobox async (nice)
        ↓
[ 1.4.0 ]  Publish checklist
```

**ADHD:** Bir sprint bitmeden diğerini açma. Yan fikir → bu dosya / todos backlog.

---

## 5) Her sprint DoD (ortak — zorunlu)

Her component sprint’i **aynı kapı**dan geçer. Eksik kalırsa sprint bitmiş sayılmaz.

| Kapı | Ne |
|------|-----|
| **API** | Controlled + uncontrolled (mümkünse); `look` / `motion` tutarlılığı |
| **A11y** | Klavye + focus; ARIA roller; reduced-motion saygı |
| **Export** | `packages/ui/src/index.ts` + types |
| **Styles** | Softglass CSS (`base.css` / look); glass = chrome, solid = uzun içerik |
| **Playground** | Sidebar section + canlı demo (`playground.tsx`) — **her sprint** |
| **Docs UI** | `apps/web/src/lib/docs.ts` prop satırları |
| **API.md + LIMITATIONS** | En az bir satır / limit notu |
| **Registry** | `generate-registry` + `registry:build` (yeni item) |
| **Test** | En az 1 smoke Vitest (aç/kapa, seç, klavye) |
| **Verify** | `typecheck` + `test` + `build:ui` yeşil |
| **Scope** | Sprint dışı bug fix yok → todos’a satır |

### Playground sözleşmesi (1.4)

Playground “sonra bakarız” değil — **her sprint’in parçası**.

1. **Sidebar:** yeni grup veya mevcut gruba net etiket  
   - `1.4a · Disclosure & nav`  
   - `1.4b · Surface`  
   - `1.4c · Structure`  
   - `1.4d · Form polish` (varsa)  
2. **Demo içeriği:** happy path + 1 edge (disabled / empty / long label)  
3. **Look strip** (uygunsa): soft / solid / glass / accent  
4. **Theme:** Aurora’da okunur; en az bir kez Obsidian’da duman testi (gözle)  
5. **Docs tab:** `docs.ts` güncel → Docs UI’da props görünür  

İsimlendirme: sidebar id’leri stabil tut (`section-accordion` gibi); URL hash ile zıplanabilsin (varsa mevcut pattern).

---

## 6) Sprint detayları

### 1.4-0 — Plan + hero (docs-only)

| | |
|--|--|
| **Neden** | README vitrin Softglass’e benzemiyor; 1.4 odaksız başlanmasın |
| **Ship** | `v1.4-plan.md` · `todos.md` 1.4 detay · `docs/assets/hero.svg` redesign · README status |
| **Yapma** | Molecule kodu, version bump, npm publish |

**DoD:** hero Aurora token’larına sadık (frost, violet accent, soft radius, 4 dil hissi); plan onaylanabilir.

---

### 1.4a — Disclosure & nav *(Must)*

| | |
|--|--|
| **Neden** | FAQ / settings / path / list sayfalama yok; kit “form kit” gibi kalıyor |
| **Branch öneri** | `feat/v1.4a-disclosure-nav` · worktree `softglass-v1.4a-disclosure` |
| **Yapma** | Mega-menu, nested breadcrumb collapse farm, infinite scroll table |

**Ship:**

| Component | Rol | API taslağı |
|-----------|-----|-------------|
| `Collapsible` | Tek panel aç/kapa | `open` / `defaultOpen` / `onOpenChange` + trigger/content |
| `Accordion` | Tek/çok panel | `type: single\|multiple`, `value`, items veya compound |
| `Breadcrumb` | Path | `items[{ label, href? }]` + separator; son current |
| `Pagination` | Sayfa | `page` `pageCount` `onPageChange`; compact + default |

**Playground (1.4a):**
- Collapsible: “Gizlilik notu” tek panel  
- Accordion: 3 FAQ item (single)  
- Breadcrumb: Home / Docs / Accordion  
- Pagination: page state + disabled uçlar  

**Test smoke:** Accordion item toggle; Pagination next/prev; Breadcrumb link count.

---

### 1.4b — Surface *(Must)*

| | |
|--|--|
| **Neden** | Empty UX + yan panel (Modal kardeşi) + hover preview eksik |
| **Branch** | `feat/v1.4b-surface` · worktree `softglass-v1.4b-surface` |
| **Yapma** | Full dashboard empty templates, bottom-sheet mobile OS clone |

**Ship:**

| Component | Rol | Not |
|-----------|-----|-----|
| `EmptyState` | İkon/title/description/actions | Molecule; solid surface default |
| `Sheet` (Drawer) | Side panel | `side: left\|right\|bottom`; portal + `usePresence`; focus trap |
| `HoverCard` | Gecikmeli preview | Popover ailesi; open delay / close delay |

**Playground (1.4b):**
- EmptyState: “Henüz proje yok” + Button action  
- Sheet: sağdan açılan ayar paneli  
- HoverCard: avatar/link üzerinde kısa kart  

**Test smoke:** Sheet open/close Escape; EmptyState renders actions; HoverCard delay (basit).

**Teknik:** Sheet = Modal floating helpers + side placement; body scroll lock dikkat.

---

### 1.4c — Structure *(Must / Should mix)*

| | |
|--|--|
| **Neden** | Wizard, toolbar, list, KPI tuğlası — ürün ekranları bunlardan kurulur |
| **Branch** | `feat/v1.4c-structure` · worktree `softglass-v1.4c-structure` |
| **Yapma** | Full wizard router, dense data toolbar, chart library |

**Ship:**

| Component | Rol | Not |
|-----------|-----|-----|
| `Stepper` | Adım göstergesi | `steps[]` `activeStep` `orientation` horizontal default |
| `Toolbar` | Aksiyon şeridi | flex row + gap; `look` glass/soft; children composition |
| `List` | ListItem host | `div`/`ul` semantics; density; optional dividers |
| `Stat` | Metric tile | `label` `value` `hint` `trend?` — Card üstü molecule |

**Playground (1.4c):**
- Stepper: 3 adımlı “Create project” (sadece gösterim + next mock)  
- Toolbar: SearchInput + Button + Badge  
- List: 3 ListItem (selected state)  
- Stat: 2–3 tile grid  

**Test smoke:** Stepper active index; List selection; Stat value text.

**Sıra notu:** List, ListItem (1.3) üstüne oturur — atomu yeniden yazma.

---

### 1.4d — Form / overlay polish *(Should)*

| | |
|--|--|
| **Neden** | 1.2 LIMITATIONS: DatePicker portal yok; MultiSelect/Combobox derinliği |
| **Branch** | `feat/v1.4d-form-polish` · worktree `softglass-v1.4d-form-polish` |
| **Yapma** | Date range, creatable free-form chaos, full async data layer |

**Ship (öncelik sırası):**

1. **DatePicker body portal** + flip/clamp (diğer overlay’ler gibi) — **Must for 1.4.0 if time**  
2. **MultiSelect** filter-in-menu (liste içinde ara) — Should  
3. **Combobox** async iskelet (`onSearch` / loading empty) — Nice  

**Playground (1.4d):**
- DatePicker: scrollable container içinde portal kanıtı  
- MultiSelect: 20+ option + filter  
- Combobox: fake async delay + loading state  

**Test smoke:** DatePicker panel `document.body` altında (query); MultiSelect filter narrows options.

**Bilinçli erteleme (tekrar):** DatePicker Calendar/Month/Year extract — refactor-only; 1.4d’de portal öncelikli, extract ayrı task.

---

### 1.4.0 publish checklist

1. Version `1.4.0` (tokens only if CSS değişti; ui her zaman)  
2. CHANGELOG `[1.4.0]`  
3. typecheck + test + build:ui + pack:check  
4. `npm whoami` = **ardaddemir** · `gh` active = **ArdaDDemir**  
5. publish **tokens (gerekirse)** → **ui**  
6. tag `v1.4.0` + push (kişisel)  
7. README component tablosu + LIMITATIONS “shipped path”  

**Not:** 1.4a–c Must biter; 1.4d kısmen kalırsa CHANGELOG’da net yaz (portal shipped / async deferred).

---

## 7) Teknik notlar

| Konu | Karar |
|------|--------|
| **Hesap** | gh ArdaDDemir, npm ardaddemir / org softglass |
| **Portal** | 1.2 `floating.ts` + `useFloatingPortal`; Sheet + HoverCard + DatePicker |
| **Presence** | Overlay exit = `usePresence` |
| **Composition** | Accordion compound (Root/Item/Trigger/Content) **veya** items API — **birini seç, karıştırma**; tavsiye: items + optional compound later |
| **Breadcrumb** | Atom `NavLink` / `Link` kullan; kendi link motoru yazma |
| **Pagination** | Server-side total bilinen basit model; cursor API yok |
| **Sheet vs Modal** | Modal = center dialog; Sheet = edge panel; API benzer (`open`/`onOpenChange`) |
| **Registry** | Her yeni molecule → generate + build |
| **Tokens** | Yeni look gerekmedikçe tokens minor bump yok; sadece ui 1.4.0 |
| **Test** | Smoke yeterli; full a11y axe suite 1.6 |

### Dosya haritası (beklenen)

```
packages/ui/src/molecules/
  accordion.tsx | collapsible.tsx | breadcrumb.tsx | pagination.tsx
  empty-state.tsx | sheet.tsx | hover-card.tsx
  stepper.tsx | toolbar.tsx | list.tsx | stat.tsx
packages/ui/src/atoms/…          # DatePicker portal touch
packages/tokens/src/base.css     # gerekirse molecule chrome
apps/web/src/components/playground.tsx
apps/web/src/lib/docs.ts
docs/API.md · LIMITATIONS.md
scripts/generate-registry.mjs · registry*.json
```

---

## 8) Karar noktaları (onayda netleştir)

1. **Accordion API:** `items[]` mi, compound (Radix-style) mi? → **Tavsiye: `items[]` first** (Softglass Select/Dropdown diliyle uyumlu).  
2. **Sheet ismi:** `Sheet` mi `Drawer` mi? → **Tavsiye: `Sheet`** (shadcn ile hizalı; docs’ta Drawer alias notu).  
3. **1.4.0 scope minimum:** a+b+c Must mi, yoksa a+b yeter mi? → **Tavsiye: a+b+c Must; d Should (portal en azından).**  
4. **Hero:** SVG kalsın mı yoksa PNG de eklensin mi? → **Tavsiye: SVG (README keskin, token-doğru); PNG opsiyonel.**

---

## 9) ADHD / odak

- Tek sprint · tek worktree  
- Playground her sprint’te — “docs later” yok  
- Yan fikir (CommandPalette, DataTable, range) → todos 1.5/1.6  
- Deep review: yazan session’da değil; ayrı session  
- Push/publish: sen onaylayınca; kişisel hesap  

---

## 10) Yeni session başlangıç komutu

```text
Softglass v1.4 — docs/claude/todos.md + v1.4-plan.md
Repo: ArdaDDemir/softglass (kişisel gh/npm)
İlk kod sprint: 1.4a Accordion + Collapsible + Breadcrumb + Pagination
main/1.3 merge sonrası worktree; work hesabına push yok
Her sprint: playground + docs.ts + registry + smoke test
```
